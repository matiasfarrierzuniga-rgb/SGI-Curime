import{useState,type FormEvent}from'react';import{affiliateRequestsService}from'../../services/affiliateRequestsService';import{getErrorMessage}from'../../utils/errors';import{Breadcrumbs,PublicPageHeader,SectionContainer,Seo}from'../../components/public/PublicComponents';const blank={fullName:'',identification:'',birthDate:'',gender:'',phone:'',email:'',address:'',occupation:'',workplace:'',affiliationReason:''}
export function AffiliationPage(){const[form,setForm]=useState(blank),[busy,setBusy]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await affiliateRequestsService.create({fullName:form.fullName.trim(),identification:form.identification.trim(),birthDate:new Date(`${form.birthDate}T12:00:00`).toISOString(),address:form.address.trim(),affiliationReason:form.affiliationReason.trim(),...(form.gender.trim()?{gender:form.gender.trim()}:{}),...(form.phone.trim()?{phone:form.phone.trim()}:{}),...(form.email.trim()?{email:form.email.trim()}:{}),...(form.occupation.trim()?{occupation:form.occupation.trim()}:{}),...(form.workplace.trim()?{workplace:form.workplace.trim()}:{})});setSuccess(true);setForm(blank)}catch(e){setError(getErrorMessage(e,'No fue posible enviar la solicitud.'))}finally{setBusy(false)}}return <><Seo title="Solicitud de afiliación" description="Solicite su afiliación a la ADI Curime."/><PublicPageHeader title="Solicitud de afiliación" intro="Complete sus datos para que la Asociación revise su solicitud."/><Breadcrumbs current="Afiliación"/><SectionContainer><div className="public-form card">{success&&<p className="message success" role="status">Solicitud enviada correctamente. La ADI revisará la información.</p>}{error&&<p className="message error" role="alert">{error}</p>}<form className="form-grid" onSubmit={submit}><label>Nombre completo<input required minLength={2} maxLength={150} value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label>Identificación<input required maxLength={50} value={form.identification} onChange={e=>setForm({...form,identification:e.target.value})}/></label><label>Fecha de nacimiento<input required type="date" max={new Date().toISOString().slice(0,10)} value={form.birthDate} onChange={e=>setForm({...form,birthDate:e.target.value})}/></label><label>Género (opcional)<input maxLength={30} value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})}/></label><label>Teléfono (opcional)<input maxLength={30} value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Correo (opcional)<input type="email" maxLength={254} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Dirección<input required maxLength={300} value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label>Ocupación (opcional)<input maxLength={100} value={form.occupation} onChange={e=>setForm({...form,occupation:e.target.value})}/></label><label>Lugar de trabajo (opcional)<input maxLength={150} value={form.workplace} onChange={e=>setForm({...form,workplace:e.target.value})}/></label><label>Motivo para afiliarse<textarea required minLength={3} maxLength={1000} value={form.affiliationReason} onChange={e=>setForm({...form,affiliationReason:e.target.value})}/></label><button className="primary" disabled={busy}>{busy?'Enviando…':'Enviar solicitud'}</button></form></div></SectionContainer></>}
