import{fireEvent,render,screen,waitFor}from'@testing-library/react';import{MemoryRouter}from'react-router-dom';import{describe,expect,it,vi}from'vitest';import{AffiliationPage}from'./AffiliationPage';import{affiliateRequestsService}from'../../services/affiliateRequestsService'
vi.mock('../../services/affiliateRequestsService',()=>({affiliateRequestsService:{create:vi.fn()}}))
describe('AffiliationPage',()=>{it('envía la solicitud pública una sola vez',async()=>{vi.mocked(affiliateRequestsService.create).mockResolvedValue({} as never);render(<MemoryRouter><AffiliationPage/></MemoryRouter>);fireEvent.change(screen.getByLabelText('Nombre completo'),{target:{value:'Ana Pérez'}});fireEvent.change(screen.getByLabelText('Identificación'),{target:{value:'1-111'}});fireEvent.change(screen.getByLabelText('Fecha de nacimiento'),{target:{value:'1990-01-01'}});fireEvent.change(screen.getByLabelText('Dirección'),{target:{value:'Curime'}});fireEvent.change(screen.getByLabelText('Motivo para afiliarse'),{target:{value:'Participar'}});fireEvent.click(screen.getByText('Enviar solicitud'));await waitFor(()=>expect(affiliateRequestsService.create).toHaveBeenCalledTimes(1));expect(await screen.findByText(/Solicitud enviada correctamente/)).toBeInTheDocument()})})
