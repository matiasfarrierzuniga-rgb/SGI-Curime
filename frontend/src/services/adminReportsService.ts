import { httpClient } from '../api/httpClient'; import type { AffiliatesSummary, AttendanceReport, JustificationsSummary, ReportQuery, SanctionsSummary } from '../types/adminReports'
export const adminReportsService={async affiliates(){return (await httpClient.get<AffiliatesSummary>('/admin-reports/affiliates-summary')).data},async attendance(params:ReportQuery){return (await httpClient.get<AttendanceReport>('/admin-reports/attendance-summary',{params})).data},async justifications(){return (await httpClient.get<JustificationsSummary>('/admin-reports/justifications-summary')).data},async sanctions(){return (await httpClient.get<SanctionsSummary>('/admin-reports/sanctions-summary')).data}}
