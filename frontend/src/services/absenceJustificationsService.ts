import { httpClient } from '../api/httpClient'; import type { Justification, JustificationList, JustificationQuery } from '../types/justifications'
export const absenceJustificationsService={async list(params:JustificationQuery){return (await httpClient.get<JustificationList>('/absence-justifications',{params})).data},async get(id:number){return (await httpClient.get<Justification>(`/absence-justifications/${id}`)).data},async approve(id:number){return (await httpClient.patch<Justification>(`/absence-justifications/${id}/approve`)).data},async reject(id:number,rejectionReason:string){return (await httpClient.patch<Justification>(`/absence-justifications/${id}/reject`,{rejectionReason})).data}}
