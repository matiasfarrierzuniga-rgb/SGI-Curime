import { useRef } from 'react'
import { Modal } from './Modal'
export function ConfirmDialog({ title, message, confirmLabel = 'Confirmar', danger = false, busy, error, onConfirm, onClose }: { title: string; message: string; confirmLabel?: string; danger?: boolean; busy?: boolean; error?: string; onConfirm: () => void; onClose: () => void }) { const confirmRef = useRef<HTMLButtonElement>(null); return <Modal title={title} onClose={onClose} busy={busy} initialFocusRef={confirmRef}><p>{message}</p>{error && <p role="alert">{error}</p>}<div className="actions"><button onClick={onClose} disabled={busy}>Cancelar</button><button ref={confirmRef} className={danger ? 'danger' : 'primary'} onClick={onConfirm} disabled={busy}>{busy ? 'Procesando…' : confirmLabel}</button></div></Modal> }
