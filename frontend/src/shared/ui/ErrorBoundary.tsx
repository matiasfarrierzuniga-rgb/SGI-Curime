import { Component, type ErrorInfo, type ReactNode } from 'react'
export class ErrorBoundary extends Component<{ children: ReactNode }, { failed: boolean }> { state = { failed: false }; static getDerivedStateFromError() { return { failed: true } }; componentDidCatch(_error: Error, _info: ErrorInfo) {} render() { if (this.state.failed) return <main className="auth-page card"><h1>Algo no salió como esperábamos</h1><p>La pantalla encontró un problema inesperado. Puedes volver al inicio e intentarlo de nuevo.</p><a className="button-link primary" href="/profile">Ir al inicio</a></main>; return this.props.children } }
